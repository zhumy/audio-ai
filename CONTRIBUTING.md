# 课程协作与维护说明

本仓库允许不同教师分别维护主题，但必须共享课程级链接、概念、符号、样式和发布规则。

## 目录责任

- `chapters/`：完整知识正文。一个主题只能有一个权威语义地址。
- `session/`：根据大纲组织宏观关系、学习顺序和跳转，不讲解具体知识。
- `labs/`：可跨路径复用的实验。
- `projects/`：综合项目和评价要求。
- `course-data/`：内容登记、当前路径、共享术语、符号和单位。
- `docs/teacher/`：维护分工和教师说明。
- `.agents/skills/`：给 Codex/Agent 的课程构建规则。

## 新增或修改 Chapter

1. 使用无顺序编号的语义目录，例如 `chapters/auditory-filterbanks/`。
2. 在 `course-data/catalog.json` 登记稳定 `id`、URL、状态和维护者。
3. 给主要叙事转折设置稳定锚点。
4. 标明前置知识、后续用途、实验和返回导览的链接。
5. 修改共享定义或符号时，同时提交术语库或符号表的修改，并请求领域负责人审查。

## 新增或修改 Session

Session 可以写：为什么这些主题在同一个学习单元出现、它们如何衔接、从哪里来、接下来用于什么。

Session 不可以复制或重新讲解 Chapter 的定义、公式、推导、例题和详细知识。它必须：

- 链接到完整 Chapter；
- 在涉及具体概念时链接到精确锚点；
- 提供可见的粘性或悬浮关系导航；
- 使用真实链接并支持键盘、触控和无 JavaScript 导航；
- 避免发布尚不存在的空白目标。

## 内容状态

- `published`：可以进入当前学生导航。
- `preview`：可预览，但不视为正式课程路径。
- `draft`：维护中，不进入公开路径。

只有 `published` 内容可以加入 `course-data/paths/current.json`。

## 定义冲突

发现冲突时不要在自己的 Chapter 中静默覆盖。请在 Pull Request 中列出两个定义和作用域，由章节维护者、领域负责人和课程负责人共同决定：统一定义、拆分概念 ID、统一符号，或明确保留不同教学层级。

## Pull Request

一个 PR 尽量只处理一个明确教学目标。说明：

- 修改了哪些学生页面；
- 是否改变定义、符号、单位或学习关系；
- 是否更新 `catalog.json`、路径和维护者；
- 如何验证链接、MathML、桌面端和移动端；
- 是否保留用户指定的权威内容。

合并前必须通过自动检查和对应维护者审查。不要直接向 `main` 提交课程正文。
